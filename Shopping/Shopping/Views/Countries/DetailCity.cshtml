@model City

@{
    ViewData["Title"] = "DetailCity";
}

<h1>Details</h1>

<div class="mb-3">
    <partial name="_CityDetails" />
</div>

<div>
    <a asp-action="EditCity" asp-route-id="@Model?.Id" class="btn btn-outline-primary">Edit</a> |
    <a asp-action="DetailState" asp-route-id="@Model?.State.Id" class="btn btn-outline-success">Go Back</a>
</div>